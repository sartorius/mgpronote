<% provide(:title, "Check") %>
<div id="mg-graph-identifier">checktag-gr</div>
<% if @resultSet.nil? %>
	//Error
<% else %>
  <script>
    var dataTagToJson = "<%= @resultSet.map{|o| Hash[o.each_pair.to_a] }.to_json %>";
    dataTagToJson = dataTagToJson.replaceAll("&quot;", "\"");
    var dataTagToJsonArray = JSON.parse(dataTagToJson);
  </script>
<% end %>
<div>
  <div class="row">
    <div class="col-lg-3">
    </div>
    <div class="col-lg-6">
      <div id="block-of-tag">
      </div>
    </div>
    <div class="col-lg-3">
    </div>
  </div>
</div>
